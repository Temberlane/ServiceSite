# Images

Drop your image files here. They're served statically and can be referenced in JSX with a root-relative path:

```jsx
<img src="/images/your-photo.jpg" alt="..." />
```

No import needed — Vite copies everything in `public/` straight to the build output.
